"""Initialize the urp package.

This file makes the `urp` directory a Python package so that modules like
`urp.agent`, `urp.core`, `urp.ledger` and `urp.knowledge_base` can be
imported from scripts outside the package. It does not need to contain
anything else.
"""

__version__ = "0.3.0"
